# Production environment for the Mentora frontend (Vercel = mode:production).
# NOTE: the Vite api.ts layer appends paths under /api/v1 (e.g. /api/v1/auth/refresh),
# so this must be the BARE origin — do NOT add /api/v1 here.
VITE_API_URL=https://mentora-backend-9.onrender.com
